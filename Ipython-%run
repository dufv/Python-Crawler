在IPython环境中，所有文件都可以通过%run命令当作Python程序来运行。
脚本是在一个空的命名空间中运行的（没有任何import，也没有定义任何其他的变量），所以其行为应该跟在标准命令环境中执行是一样。
此后，该文件所定义的全部变量（还有各种import，函数和全局变量），就可以在当前IPython shell中访问了（除非发生了异常）。
如果希望脚本能够访问交互式IPython命名空间中定义的变量，就应该使用%run -i。
